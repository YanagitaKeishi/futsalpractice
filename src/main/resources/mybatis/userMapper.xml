<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.example.app.dao.UserDao">

	<select id="selectAll" resultType="com.example.app.domain.User">
		SELECT *,user_types.name AS user_type_name 
		FROM  users
		JOIN users.user_type_id = user_types.id;
	</select>
	
	<insert id="insert">
		INSERT INTO users (name, member_id, tel, user_type_id, plan_id) 
		VALUES (#{name}, #{memberId}, #{tel}, #{userType.id},#{planId})
	</insert>
</mapper>